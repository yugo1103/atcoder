#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;
#define rep(i, n) for(int i = 0; i < n; i++)

int main(void){
    ll k;
    cin >>  k;

    cout << floor(k / 2.0) * ceil(k / 2.0) << endl;
    return 0;
}
